import atelier.text;
import std.test;

#[test]
void span_widths() {
    Span s = Span.new("hello");
    assert_eq_i64(5, s.width(), "ascii width");
    Span w = Span.new("éあ");
    assert_eq_i64(3, w.width(), "mixed width");
}
